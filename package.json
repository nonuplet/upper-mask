{
  "name": "upper-mask",
  "version": "1.0.0",
  "description": "Plugin to allow masking with shape on top",
  "main": "main.js",
  "scripts": {
    "dev": "tsc -w",
    "build": "yarn lint && tsc -p tsconfig.json",
    "lint": "biome check --apply main.ts"
  },
  "author": "kokonoe",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "^1.3.3",
    "@figma/plugin-typings": "^1.81.0",
    "typescript": "*"
  }
}
